# AWX Analytics Portal – API dependencies
# Fully pinned for reproducible installs.
# Generated for Python 3.9+ on RHEL 8/9.
#
# Install:
#   python3 -m venv /opt/awx-portal/venv-api
#   /opt/awx-portal/venv-api/bin/pip install \
#       --no-index --find-links /opt/awx-portal/wheels/api \
#       -r api/requirements.txt
#
# Or with internet access:
#   pip install -r api/requirements.txt

# ── ASGI framework ────────────────────────────────────────
fastapi==0.111.0
starlette==0.37.2
pydantic==2.7.1
pydantic-core==2.18.2
pydantic_settings==2.2.1
annotated-types==0.6.0
typing_extensions==4.11.0
python-multipart==0.0.9
email-validator==2.1.1

# ── ASGI server ───────────────────────────────────────────
uvicorn[standard]==0.29.0
gunicorn==22.0.0
httptools==0.6.1
uvloop==0.19.0
websockets==12.0
watchfiles==0.21.0

# ── Database ──────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Config ────────────────────────────────────────────────
PyYAML==6.0.1
python-dateutil==2.9.0
six==1.16.0

# ── HTTP client (used in auth.py OAuth2 flow) ─────────────
requests==2.31.0
urllib3==2.1.0
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6

# ── Swagger UI (bundled with FastAPI) ─────────────────────
anyio==4.3.0
sniffio==1.3.1
h11==0.14.0
click==8.1.7